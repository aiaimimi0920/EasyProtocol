# RustProtocol Client Interface Skeleton

This file records the current client-side boundary skeleton for `RustProtocol`.

Potential concerns:

- normalized request ingestion from `EasyProtocol`
- Rust-oriented request translation
- response handoff back to the outward service
