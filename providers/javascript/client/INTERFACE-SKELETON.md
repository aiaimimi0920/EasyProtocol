# JSProtocol Client Interface Skeleton

This file records the current client-side boundary skeleton for `JSProtocol`.

Potential concerns:

- normalized request ingestion from `EasyProtocol`
- JavaScript-oriented request translation
- response handoff back to the outward service
