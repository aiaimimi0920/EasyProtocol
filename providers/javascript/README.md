# JSProtocol

`JSProtocol` is the language-specific protocol service for JavaScript usage.

Current focus at the skeleton level:

- JavaScript-oriented protocol invocation boundary
- transport placeholders
- runtime placeholders
- local service-specific tests and docs

No executable implementation has been added yet.
