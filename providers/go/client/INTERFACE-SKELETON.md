# GolangProtocol Client Interface Skeleton

This file records the current client-side boundary skeleton for
`GolangProtocol`.

Potential concerns:

- normalized request ingestion from `EasyProtocol`
- Go-oriented request translation
- response handoff back to the outward service
