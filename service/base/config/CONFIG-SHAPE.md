# EasyProtocol Config Shape

This file records the configuration skeleton for `EasyProtocol`.

Potential configuration areas:

- outward service bind settings
- registry source settings
- enabled language-service list
- routing defaults
- diagnostics and tracing switches

No concrete config format has been fixed yet.
