# EasyProtocol Public Endpoints

This file records the current public endpoint skeleton.

Potential public endpoints:

- request submission
- capability inspection
- health inspection
- stats inspection

The outward public API should remain unified regardless of which language
service handles the request.
